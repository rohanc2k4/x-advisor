.DS_Store
__pycache__/
*.pyc
*.pyo

# Local-only Claude Code config
.claude/settings.local.json

# Personal voice training data (each user seeds their own)
voice/*.md
voice/*.txt
!voice/README.md

# Generated outputs stay local
outputs/*
!outputs/.gitkeep

# Optional niche allowlist is local
niche.yaml

# Local config (handle, timezone, cached user_id, tier)
config.yaml

# Cached state (resolved user_id, last-seen IDs, etc.)
.cache/
