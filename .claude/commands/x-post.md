---
description: Post a drafted message via x-mcp. Requires typed `post` confirmation.
argument-hint: <draft-text> | latest
---

Invoke the [[x-post]] skill with the user's draft as the argument: `$ARGUMENTS`. If `latest`, reads from today's `outputs/x-drafts-YYYY-MM-DD.md`. Typed `post` confirmation required.
