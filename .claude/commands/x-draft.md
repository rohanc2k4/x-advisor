---
description: Draft a single X post in your voice on a given topic.
argument-hint: <topic>
---

Invoke the [[x-draft]] skill with the user's topic as the argument: `$ARGUMENTS`. Reads `voice/` for tone, drafts a candidate, diff-and-approve gate.
