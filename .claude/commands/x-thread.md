---
description: Draft a multi-post thread on a topic. Default 5 posts.
argument-hint: <topic> [--length N]
---

Invoke the [[x-thread]] skill with the topic + optional `--length N`: `$ARGUMENTS`. Reads `voice/` for tone. Posting is manual paste (see skill caveat).
